# Full-resolution PNG captures — large and re-creatable with capture-screens.mjs.
raw/
# Build artefacts: manual.html and the sliced page figures are regenerated by
# build-manual.mjs from web/ on every run. The PDF it produces is committed one
# level up, at docs/Universe3-Underwriter-Manual.pdf.
screens/
manual.html
# Cached Playwright session used by the capture script.
.auth-state.json
node_modules/
